/** Bounds fan-out so one product with many industries/buyer types/countries can't flood the queue. */
export const MAX_INDUSTRIES_PER_PRODUCT = 5;
export const MAX_BUYER_TYPES_PER_PRODUCT = 5;
export const MAX_COUNTRIES_PER_WORKSPACE = 10;
